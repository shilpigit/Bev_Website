(function () {
    'use strict';

    define(
        function () {

            var model = function (options) {

                this.info = options.info;
            };

            return model;
        });
})();