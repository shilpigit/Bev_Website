(function () {
    'use strict';

    define(
        function () {

            var model = function (options) {

                this.companies = options.companies;
            };

            return model;
        });
})();